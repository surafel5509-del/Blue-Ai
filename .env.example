# Required: long random secret used to encrypt provider keys in an HttpOnly cookie
APP_ENCRYPTION_KEY=replace-with-a-long-random-secret

# Optional: public URL used by OpenRouter attribution
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional future web-research provider
# TAVILY_API_KEY=your-key